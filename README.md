# Event Agency Website

This repository contains a simple Flask application for an event agency. It includes a few example pages and basic styling.

## Setup

1. Install dependencies:
   ```bash
   pip install -r requirements.txt
   ```

2. Run the application:
   ```bash
   python app.py
   ```

Visit `http://localhost:5000` in your browser.

